# =================================
# SecDash VPN Configuration
# =================================

# Server Public IP or Domain (REQUIRED!)
# This must be the public IP address of your server
# that mobile clients will connect to.
# DO NOT use 'auto' in production - always set explicit IP.
# Example: WG_HOST=101.47.128.101
WG_HOST=auto

# WireGuard Port (UDP)
# Default is 51820. Make sure this port is open in your firewall.
WG_PORT=51820

# Web Dashboard Port (TCP)
# Default is 3000.
WEBUI_PORT=3000

# WireGuard Subnet
# The internal IP range for VPN clients.
# First usable IP (.1) is the server, clients get .2, .3, etc.
WG_SUBNET=10.252.1.0

# Timezone
TZ=Asia/Jakarta

# Admin Password (only used on first run if database is empty)
# Change this in the UI after first login.
ADMIN_PASSWORD=password

# Cookie Settings
# Set to 'true' ONLY if you are serving over HTTPS.
# If using HTTP (like IP address), keep as 'false'.
COOKIE_SECURE=false
