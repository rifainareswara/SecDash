# =================================
# SecDash VPN - DEVELOPMENT Environment
# =================================

# Environment identifier
NODE_ENV=development
ENV_NAME=dev

# Server Public IP or Domain
WG_HOST=auto

# WireGuard Port (UDP) - DEV uses 51820
WG_PORT=51820

# Web Dashboard Port (TCP) - DEV uses 3000
WEBUI_PORT=3000

# WireGuard Subnet - DEV uses 10.252.1.x
WG_SUBNET=10.252.1.0

# Docker container names (to avoid conflicts)
WG_CONTAINER_NAME=wireguard-dev
DASHBOARD_CONTAINER_NAME=vpn-dashboard-dev

# Timezone
TZ=Asia/Jakarta

# Admin Password
ADMIN_PASSWORD=password

# Cookie Settings (false for development)
COOKIE_SECURE=false
