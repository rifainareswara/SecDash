# =================================
# SecDash VPN - PRODUCTION Environment
# =================================

# Environment identifier
NODE_ENV=production
ENV_NAME=prod

# Server Public IP or Domain
# WAJIB diisi dengan IP server PRODUCTION
WG_HOST=YOUR_PRODUCTION_IP

# WireGuard Port (UDP) - PROD uses 51822
WG_PORT=51822

# Web Dashboard Port (TCP) - PROD uses 3002
WEBUI_PORT=3002

# WireGuard Subnet - PROD uses 10.252.3.x
WG_SUBNET=10.252.3.0

# Docker container names (to avoid conflicts)
WG_CONTAINER_NAME=wireguard-prod
DASHBOARD_CONTAINER_NAME=vpn-dashboard-prod

# Timezone
TZ=Asia/Jakarta

# Admin Password (GANTI dengan password yang kuat!)
ADMIN_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# Cookie Settings (set true jika menggunakan HTTPS)
COOKIE_SECURE=true
